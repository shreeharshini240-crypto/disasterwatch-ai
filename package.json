{
  "name": "disasterwatch-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready disaster intelligence platform with real-time alerts, maps, analytics, and AI risk assessment.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace @disasterwatch/api\" \"npm run dev --workspace @disasterwatch/web\"",
    "build": "npm run build --workspace @disasterwatch/db && npm run build --workspace @disasterwatch/api && npm run build --workspace @disasterwatch/web",
    "start": "concurrently \"npm run start --workspace @disasterwatch/api\" \"npm run start --workspace @disasterwatch/web\"",
    "lint": "npm run lint --workspace @disasterwatch/web && npm run lint --workspace @disasterwatch/api",
    "db:generate": "npm run db:generate --workspace @disasterwatch/db",
    "db:migrate": "npm run db:migrate --workspace @disasterwatch/db",
    "db:seed": "npm run db:seed --workspace @disasterwatch/db"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "overrides": {
    "postcss": "^8.5.10",
    "next": {
      "postcss": "^8.5.10"
    }
  }
}
